# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
.idea/
.vscode/

# Python pipeline cache
__pycache__/
*.pyc

# scratch: Z-Image contact sheets + raw image picks (kept local, never shipped)
tools/_zpick/

# never commit the heavy originals (they live in sibling folders, not here)
*.zip
*.exe
*.keras
*.mp4
# ...except the small web-optimized showcase clips that the Art gallery serves
!assets/video/art/*.mp4
